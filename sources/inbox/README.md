# sources/inbox/ — 临时中转

来源/分类不明、或各渠道临时丢进来的东西，先落这里，**不是长期存放地**。

## 归档三步走

1. **看**：读内容，判断真实来源。
2. **归类**：决定它属于 `feishu/<租户>/...`、`wechat/`、`mp/`、`social/` 还是 `knowledge/`。
3. **落位**：移动过去，补全该来源要求的 frontmatter，重命名为规范文件名。

归档后 inbox 里就不该再留着它。

> `_example-*.md` 为示例，采用后删除。

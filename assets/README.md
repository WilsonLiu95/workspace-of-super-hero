# assets/ — 素材

图片、图表、截图等，被交付物（尤其是 `deliverables/web/` 的 HTML 和文档）引用。

- 建议按交付物或主题分子目录，便于清理。
- 在文档/HTML 里用相对路径引用，例如 `../../assets/<name>.png`。
- 若你的环境配置了生图技能/工具，可把生成的图存这里再引用（**本模板不假设它存在**）。

> 注意：图片可能含敏感信息；如有需要，自行在 `.gitignore` 增加规则。

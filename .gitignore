# --- OS / 编辑器垃圾 ---
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# --- 密钥/凭据：永不提交 ---
# 真实凭证放 .env.local（pull-*.sh 会自动 source）；.env.example 是模板，保留跟踪。
.env
.env.local
.env.*.local
*.key
*.pem
secrets.*
credentials.*

# --- 临时/依赖 ---
.tmp/
tmp/
node_modules/
__pycache__/
*.pyc

# --- Agent 兼容入口 ---
# .claude/skills 与 .agents/skills 是指向根 skills/ 的软链接，需要随模板跟踪；
# .agents/ 下其它本机态继续忽略。
.agents/*
!.agents/skills

# --- 腾讯 WorkBuddy 本地态 ---
# 如需 WorkBuddy skill 入口，可用 .workbuddy/skills -> ../skills；其它本地态不跟踪。
# WorkBuddy 的工作区契约直接复用根 AGENTS.md（无 WORKBUDDY.md 时自动加载它）。
# 个人/含凭证的本地覆盖（WORKBUDDY.local.md、settings.local.json）永不提交。
.workbuddy/*
!.workbuddy/skills
WORKBUDDY.local.md

# --- 原始来源数据：保留结构+示例，忽略真实数据 ---
# 采用者放进 sources/ 的真实资料（私密聊天/文档）默认留在本地、不进 git，
# 避免误传隐私。模板自带的脚手架（README、.gitkeep、明显虚构的 _example-* 示例）会被保留。
# 如确需版本化真实来源数据：自行删除/调整下面规则，且远端仓库必须设为私有。
sources/**
!sources/**/
!sources/**/README.md
!sources/**/.gitkeep
!sources/**/_example-*
!sources/**/_example-*/**
